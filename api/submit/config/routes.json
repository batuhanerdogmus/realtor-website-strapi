{
  "routes": [
    {
      "method": "GET",
      "path": "/submits",
      "handler": "submit.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/submits/count",
      "handler": "submit.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/submits/:id",
      "handler": "submit.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/submits",
      "handler": "submit.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/submits/:id",
      "handler": "submit.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/submits/:id",
      "handler": "submit.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
